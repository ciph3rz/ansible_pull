---
- name: Install Desktop applications
  apt:
    pkg:
      - gnome-tweaks
      - flameshot
      - vlc
      - gnupg
      - jq
      - python3-psutil
      - transmission
    state: present
    update_cache: yes
  when: ansible_pkg_mgr == 'apt'
